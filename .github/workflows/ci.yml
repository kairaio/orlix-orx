name: ORX CI

on:
  push:
    branches: [main, orx-core-mvp]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: orx
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d orx"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/orx
      REDIS_URL: redis://localhost:6379/0
      ADMIN_API_KEY: test-admin-key

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest httpx

      - name: Compile Python
        run: python -m compileall app alembic

      - name: Run migrations
        run: alembic upgrade head

      - name: Run tests
        run: pytest -q
